.site-brand-wrapper {
  position: relative;
}

.site-meta {
  padding: 20px 0;
  color: white;
  // background: $black-deep;
  background-image: url(http://news.mydrivers.com/Img/20120128/2012012810392885.jpg);
  background-position: center;
  background-size: 100%;

  +tablet() {
    box-shadow: 0 0 16px rgba(0,0,0,0.5);
  }
  +mobile() {
    box-shadow: 0 0 16px rgba(0,0,0,0.5);
  }
}
.site-meta .custom-logo-site-title{
  background: rgba(0,0,0,.5);
}
.brand {
  padding: 0;
  background: none;

  &:hover { color: white; }
}

.site-subtitle {
  margin: 10px 10px 0;
  font-weight: initial;
}

.site-search form { display: none; }
